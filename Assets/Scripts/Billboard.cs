using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;

public class Billboard : MonoBehaviour {
  private Transform lookAt;

  void Start() {
    // lookAt = Globals.Instance.playerHead.transform;
  }

  void Update() {
    // if (lookAt != null) {
    //   transform.LookAt(lookAt);
    // }
  }
}
